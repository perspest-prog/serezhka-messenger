<div class="{{classes.container}}">
  <label class="{{classes.label}}" for={{name}}>{{labelValue}}</label>
  {{#if isValid}}
    <input class={{classes.input-validate}} type={{type}} name={{name}} id={{name}} value="{{value}}">
  {{else}}
    <input class="{{classes.input-validate}} {{classes.not-valid}}" type={{type}} name={{name}} id={{name}} value="{{value}}">
  {{/if}}
  <span class={{classes.error}}>{{error}}</span>
</div>