<div>
  <form>
    <div class={{classes.form_container-profile}}>
      {{#each inputs}}
      {{{this}}}
      {{/each}}
    </div>
    <div class={{classes.button_container-profile}}>
      {{#each buttons}}
      {{{this}}}
      {{/each}}
      {{{link}}}
    </div>
  </form>
</div>